import { Helmet } from 'react-helmet';
import { Link, NavLink } from 'react-router-dom';
import { useAuth } from '../../hooks/useAuth';
import { TitleWrapper, Container } from './Home.styled';

const Home = () => {
    return ();
};

export default Home;
